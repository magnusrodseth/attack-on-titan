# Wayfinder Map: First-person ODM titan-slaying game

Label: `wayfinder:map` · Tracker: local markdown (`wayfinder/tickets/`) · No remote configured.

## Destination

A playable, fun, replayable browser game: first-person Attack on Titan wave survival in a procedural low-poly city, built with Three.js + TypeScript, simulation core developed test-first (vitest), verified live in Chrome via playwriter. `pnpm dev` runs it; `pnpm test` is green.

## Notes

- Skills in play: find-docs (Three.js API via ctx7), tdd (sim core seams), playwriter (browser verification).
- **This effort carries execution in-map** (per wayfinder Notes override): tickets include build tasks, and multiple tickets may be resolved in one autonomous session, since the user requested the finished game and is AFK. HITL grilling is replaced by documented decisions with rationale; the user can reopen any ticket to overturn a decision.
- Agreed test seams (tdd skill): pure sim modules only — `rng`, `rope`, `player`, `titan`, `combat`, `waves`, `score`, `upgrades`, `game`. Renderer/HUD are thin adapters verified by playwriter smoke test, not unit tests.

## Decisions so far

- [What makes ODM movement fun](tickets/001-odm-feel-research.md) — hard pendulum constraint that keeps tangential momentum, gas thrust toward anchors, speed-scaled nape kills; sourced from AOTTG + grapple-physics writeups (assets in `docs/research/`).
- [Stack and scaffold](tickets/002-stack.md) — Vite + TypeScript strict + three + vitest, pnpm, no physics engine (hand-rolled PBD rope is simpler and unit-testable).
- [Core loop and replayability design](tickets/003-core-loop-design.md) — wave survival, seeded city, gas/blade resources with resupply risk-reward, style-based combo scoring, pick-1-of-3 roguelite upgrades between waves, localStorage bests, shareable seeds.
- [ODM physics sim](tickets/004-rope-physics.md) — PBD rope + player integrator, 30 tests; browser play exposed ground-pinning and momentum-killing landings, both fixed test-first (skid model).
- [Titans and combat](tickets/005-titans-combat.md) — wander/chase/attack/leap state machine, windup swats with invuln window, speed-scaled nape damage, blade wear.
- [Progression](tickets/006-progression.md) — per-wave RNG streams keep seeds deterministic; score multipliers compose; bests persist.
- [Renderer](tickets/007-renderer.md) — AoT district look per user reference images (instanced gabled row-houses, towers, 50m wall), procedural grinning pure titans, ropes/streaks/shake/FOV effects, full DOM HUD.
- [Browser verification](tickets/008-browser-verify.md) — full loop verified live in Chrome via playwriter debug hook with screenshot evidence; 73 tests green, tsc clean, production build 144 kB gzip.
- Controls v2 (user request, 2026-07-09) — Space jump, Shift horizontal boost; no reel button: holding a hook winches automatically at a speed-scaled rate with a ratcheting rope, releasing detaches. Velocity steering on slides/air fixed slipperiness. 3 spare gas canisters auto-swap on empty. Houses raised to 14-22m for bigger pendulum drops. First-person blade sweep viewmodel; volumetric rope cables; Cloister Black titles.
- Titan interaction rework (user request, 2026-07-09) — hooks attach to titans with anchors that track them; ankle-slice system (both ankles → 60s kneel with lowered nape, then full-health rise); titans collide with buildings and navigate corridors; 30% slower with rate-limited turning. Speed rebalanced to physics research: killSpeed 17 m/s, cap 40 m/s, air-only boost.
- Texture mandate (user rule, 2026-07-09) — recorded in repo CLAUDE.md: every visible surface texture must be a sourced free online asset (subagent-verified, CC0), never an invented flat color. City fully textured via scout results (Poly Haven + OpenGameArt): plaster/brick walls, terracotta/slate roofs, cobblestone, castle wall, rock mountains, bark/leaf trees, photo windows.
- Combat and mobility v3 (user requests, 2026-07-09) — ankle-cripple system; bigger nape hitbox; Q focus slow-mo (drain/regen meter, muffled audio); Shift click-burst dash along look direction; hold-W never adds speed above run speed (momentum is king); swing-carry tuning (gravity -19, halved quadratic drag, stronger low-speed winch); boost swaps canisters when the tank is low (bugfix); sloped roof/spire collision with real hook raycasts; circular minimap with titan blips; capsule-limbed titans with photo-decal faces (CC0 eyes/teeth sprites).
- Music (user pick from scout audition, 2026-07-09) — "Five Armies" (Kevin MacLeod, CC-BY, credited) alternating with "Eclipse Legion" (CC0) through the master chain.
- Tethered ground momentum (user request, 2026-07-09, revised same day) — grounded runs cost nothing per second while a hook is attached: a tethered touchdown banks the swing speed and dents the run 8%, liftoff (or a jump) hands the bank back, and releasing while grounded forfeits it to the normal skid. Running past the anchor scoops the player back into the swing (emergent from the rope constraint plus winch, pinned by tests).
- Health economy (user request, 2026-07-09) — full heal when a new wave starts and at resupply; each titan kill returns one heart (capped at max), flagged on the kill event for HUD feedback.
- Menu branding (user request, 2026-07-09) — Cloister Black titles, Cinzel subtitles/labels/buttons, Alegreya body (both SIL OFL, self-hosted woff2 in public/fonts); chamfered plate buttons and upgrade cards built as clipped ::before plates with drop-shadow brass rims that follow the silhouette; larger menu type throughout; settings menu (music/SFX buses in AudioSystem, mouse sensitivity multiplier) persisted under 'aot-odm-settings'.
- Game mode system (user request, 2026-07-09) — `src/sim/modes.ts` GameMode interface (start/step/chooseUpgrade hooks over shared core systems); waves extracted as the first registry entry with parity and determinism pinned by tests; menu Game Mode panel selects a mode, persisted under 'aot-odm-mode' and carried in the ?mode= URL param (switching reloads). Future modes (time trial, parkour rings) plug in by appending to GAME_MODES.
- Run persistence (user request, 2026-07-09) — `src/sim/persist.ts` serializes the whole run (player incl. mutated config, hooks with titan anchors, titans, score, offers by id, focus, camera view, and the live rng stream via resumable state in rng.ts) versioned under 'aot-odm-run'; autosaved ~1 Hz + on pagehide/visibility-hidden; restore is validated against seed+mode+version and pinned bit-for-bit by a divergence test; death clears the slot. Local-first stance until multiplayer: all meta-state stays behind small keyed localStorage seams (see IDEAS.md for the PartyKit + Neon/Drizzle direction).
- Tethered run tuning v2 (user request, 2026-07-09) — the touchdown dent is gone (the graze is free; the bank remains as a liftoff floor), and holding a direction while hooked on the ground now ADDS speed (+6 m/s²): sprinting the arc bottom banks velocity. Winch ~50% faster (reelSpeed 10, higher low-speed floor).
- Titan pathfinding (user request, 2026-07-09) — grid A* over a baked street NavGrid (`src/sim/nav.ts`: 2m cells, building footprints inflated 1.6m, no corner cutting, line-of-sight smoothing) instead of a navmesh: the city is axis-aligned rectangles, so a grid is deterministic, cheap and unit-testable. Chasing titans follow waypoints (repath 1.25s), spawns snap to walkable streets, physically-embedded titans wade out, and the arena is now actually passed to stepTitan (the original bug: collision never ran in-game). At most MAX_CHASERS=3 titans hunt at once via sticky distance-ranked tokens so the player is never mobbed.
- Kill feel and empty-resource feedback (user request, 2026-07-09) — every kill: blood + steam double burst, deep kill-hit layer, ~90ms hitstop; aberrant kills additionally pay a 1.75× rarity bonus, "Aberrant Slain!" banner, a sting (scouted CC0), 140ms hitstop. Ankle slices get their own blood burst at the cut ankle (event carries the side). Slashing with zero blades jams the viewmodel (aborted flick) instead of sweeping; boosting truly dry likewise: both emit an 'empty' sim event with a click sound and a "Resupply!" popup.
- Mid-run restart (user request, 2026-07-09) — "Restart Run" plate in the pause menu (hidden on the fresh-start screen): clears the run save and restarts wave 1 on the same seed and mode.
- Mobile gate (user request, 2026-07-09) — touch-only devices (pointer coarse + hover none) get an on-brand "Halt, Soldier" overlay that blocks play and points to desktop with keyboard and mouse; beginRun double-guards it.
- HUD gauges v2 (user request + reference image, 2026-07-09) — health lives in the bottom-left resource stack with empty-heart pips; gas and blade bars are segmented into countable uses (gas cells = boost taps of BOOST_COST, blade cells = durability, both tracking upgrades), focus stays continuous; brass-framed gauges with gold/steel/violet fills and Cinzel labels.
- Blade viewmodel v2 (sword scout + user reference images, 2026-07-09) — scout found no crossguard-free CC0 sword model, so the ODM blade is a hand-built extruded profile (long thin single-edged bar, diagonal tip clip, three spine notches, trigger grip) skinned with ambientCG Metal012 brushed steel (CC0); sweep animation untouched on the pivot. Default hearts raised 3 → 5.
- Audio (user request, 2026-07-09) — CC0 samples from OpenGameArt (creature SFX by rubberduck, sword whooshes by StarNinjas) for voiced sounds; procedural WebAudio for speed-tracked wind, gas hiss, impacts and chimes; distance-attenuated ambient titan roars. `src/audio.ts`, credits in README. Skipped a CC-BY pack to stay attribution-free at the asset level.
- Thunder Spear secondary weapon (grilled with user, 2026-07-09) — the "weapon class system" idea graduated here with a terminology reversal: there is NO class system (the registry-like seam was explicitly rejected; see CONTEXT.md). The Thunder Spear is a universal consumable secondary every run carries alongside blades: fire on KeyE/middle-mouse edge (0.6s refire), straight 60 m/s rocket, 120m fizzle, sticks into whatever it hits first (titan part via titan-local anchor, building, street) and always detonates after a 2.5s beeping fuse. One 5m blast radius: nape center inside = kill at any speed (flat 75 points, rarity + chain multipliers only, still returns a heart and feeds the combo); other titans in radius take 60 damage plus the new `staggered` behavior (~3s frozen, wounds kept — deliberately not `crippled`, which heals on rising; leaping/crippled titans exempt). Player in radius loses 1 heart + blast knockback. Economy: start 2/2, resupply does NOT restock spears — the only source is 3 per-wave street caches (`seed:spears:wave`, +1 each, ~3m fly-through collect, despawn with the wave, amber minimap blips); "Reinforced Spear Racks" upgrade (+2 capacity, stacking toward the canon 8). Sim in `src/sim/spear.ts` (+ stagger in `titan.ts`), SAVE_VERSION 2 (v1 saves discarded), co-op explicitly out of scope. Assets reuse credited steel/metal/linen textures; procedural beep/boom audio.
- Day/night cycle with a realistic sky (user idea, 2026-07-09; graduated from IDEAS.md) — deterministic time-of-day in `src/sim/daynight.ts` (tested): seeded start hour via `hashSeed(seed + ':daynight')` pinned to daylight (0.3–0.6 of the day), one full cycle per 10 min of `g.time`, so the clock replays per seed and survives the run save with zero new state; co-op inherits it since `stepCoopClient` advances `g.time`. Renderer in `src/render/daynight.ts`: Three.js `Sky` (Preetham) driven by sun direction with dusk-boosted rayleigh/mie for golden sunsets; ONE directional light plays sun by day and hands over to a cool moon light at night (single shadow map); hemisphere light and fog grade from the original day palette to dark-blue night through a warm dusk band; camera-tracking star dome (NASA Deep Star Maps 2020, public domain, 4K EXR converted to 2K JPEG — kept above the 512px texture norm because it spans the whole sky; renderOrder forced behind other transparents since the camera-centered dome defeats the distance sort, UV poles tilted to the horizon to hide the pinch) plus a textured moon (NASA CGI Moon Kit), both fading in after sunset; window lamps ramp from 30% to ~68% lit with a stronger overbright glow while unlit window glass and cloud billboards (both unlit MeshBasicMaterial) are hand-darkened. Browser-verified at noon/sunset/dusk/midnight via new `__aot.setClock(fraction|null)` and `__aot.setView(yaw, pitch)` debug hooks.
- Playable night + automatic flashlight (user request, 2026-07-09): night regraded from near-black to deep dusk (brighter night fog/hemisphere palette, moon light 0.4 → 0.95, gentler fog pull-in) so the district stays playable at midnight. On top rides an automatic flashlight, no player control by design: `src/sim/flashlight.ts` (tested) lights the beam once the sun is under the horizon (nightFactor ≥ 0.5), burns a 180 s battery only while lit, and only a resupply recharges it. The sun is down ~300 s of every 600 s cycle, so a full charge dies mid-night and exactly one station trip covers the rest (pinned by test). Battery lives on PlayerState, survives the run save, and drains identically in co-op via the shared `stepLamp` in game.ts (client-local resource; the server never sees it; the server resupply ack refills it). Renderer `src/render/flashlight.ts`: warm SpotLight riding the camera, near-total penumbra so the pool dissolves at its edge (user-tuned), guttering flicker below half glow, no shadow casting. HUD gains a night-only LAMP gauge joining the low-resource red pulse under 20%, plus Lamp Fading / Lamp Dead banners off new lampLow/lampDead sim events. The dev playground drawer gained a time-of-day slider (drives the render-only clock override via a new DevCtx.setClock) and a live battery slider.
- Bigger weak-point hitboxes + visible ankle targets (user request, 2026-07-09): nape radius 0.75 → 0.85 slashRange and 0.07 → 0.09 height share; ankle radius 0.4 → 0.5 slashRange and 0.02 → 0.035 height share. The grown ankle radius meant both ankles could be in range at once, so the cut now lands on the nearer one instead of whichever the loop checked first. Ankles now advertise themselves like the nape: red emissive heel boxes on each lower leg sharing the pulsing nape material, hidden per-ankle once cut and entirely while crippled or dead.
- Weak points as bloom, not boxes + second hitbox growth (user request, 2026-07-09): the red indicator boxes became a red bloom bleeding from slightly within the flesh ("indicate, don't show"). Each weak point in `src/render/titans.ts` is a two-sprite group on a shared canvas radial gradient (procedural is fine: gameplay indicator glows are the accepted texture-rule exception): an additive halo whose center sits inside the body so the flesh swallows the core and only the glow escapes, plus a small normal-blended stain riding the surface, added because additive light vanishes against a flashlight-lit surface. Hitboxes grew again: nape 0.85 → 0.95 slashRange and 0.09 → 0.11 height share; ankle 0.5 → 0.6 and 0.035 → 0.05. At that size the ankle radius spans both feet of a mid-height titan, so the re-slice test now pins that the second cut lands on the other tendon, never the already-cut one. Verified in the playground at noon, dusk-night, and under the flashlight at point-blank. The builders are exported (makeWeakPointMats/makeWeakPoint) and the footballer figures reuse them: the Striker and Captain carry the same nape bloom plus heel tendons, since they are titans with aberrant behavior. A follow-up anchored every bloom on the actual body surface (skull radius per titan, hair crown on the footballers, calf on the heels) after the fixed-depth anchors left a floating red bubble behind small-headed titans.
- Matchday: footballers as live event titans (grilled decisions, 2026-07-09): every 3rd wave is matchday, guaranteed, with Haaland (striker) and Kane (captain) spawning on top of the normal horde at their signature 13 to 16 m. New TitanKinds 'striker' and 'captain' share the titan state machine but out-class the abnormal via the KIND_STATS profile in sim/titan.ts: aggro 160 (abnormal 130), walk 0.44h (0.38h), turn 2.5, swat rest 0.9 s, leap apex 17 m/s (13). One-cut still works: threat is aggression, not HP. Kills pay a 3x rarity multiplier (abnormal 1.75x), banner "Striker/Captain Sent Off!". Announced by announceWave in main.ts ("Matchday · Wave N" + boom); footballers are white blips on the minimap. Renderer: TitanVisual's pose state machine was extracted into TitanPoser driving a TitanPuppet interface; buildFootballer exposes a puppet, FootballerVisual (strikers.ts) drives it, and the kind-aware TitanPool moved to render/titanPool.ts to keep titans.ts and strikers.ts acyclic. Co-op inherits everything through the shared waveComposition (Worker redeploy required). Hooks anchoring to titans already existed (rope.ts attachHookToTitan + updateTitanAnchor, nearest-hit priority vs buildings, tear-free on death, persisted, shared with co-op).
- Thunder Spear merge (handoff, 2026-07-09): branch worktree-weapon-class-system landed on main via a re-merge (main had moved 5 commits since the branch's base). Keep-both resolutions: TitanKind footballers + the 'staggered' behavior coexist; the branch's stagger pose was ported into the shared TitanPoser (so footballers reel from blasts too); spear imports joined titanPool/isFootballer/isMatchday in main.ts. One seam reconciled post-merge: registerSpearKill now pays the footballer 3x tier (75 → 225), matching blade kills. Verified via playwriter headless: 2-spear loadout + 3 caches on start, fire → stick (2.5 s fuse) → detonate → stagger → kill (weapon 'spear', 75 pts, heart back), pickup refill, self-damage knockback, resupply refills the lamp but never spears, and a pinned striker spear kill paying 225. Fun find: an unpinned striker dodges the spear by leaping at you. Worktree and branch cleaned up; NOT pushed (user tests locally first).
- Thunder spears in multiplayer (grilled decisions, 2026-07-10): server-authoritative spears in the shared world. Decisions: friendly fire is knockback only (teammates in the blast get thrown, no heart lost; the firer pays a heart like solo), caches scale 3 + 2 per extra connected player, first come first served. CoopWorld carries spears/pickups plus a spearId → owner map; coopFire is a client intent carrying the client-owned aim (no rewind: flight time dwarfs latency); stepCoopSpears reuses stepSpears (playerPos made nullable) and resolves blasts per soldier; kills pay registerSpearKill to the owner incl. the footballer 3x tier; kill events carry weapon on both loops. Client: fire edge in stepCoopClient with dry-click prediction, syncSpearMirror interpolates spears and copies caches into g.spears/g.pickups so SpearsView, minimap diamonds, HUD gauge and fuse beeps work untouched; new blasted event applies knockback client-side. Protocol widened additively ('fire' intent, snapshot spears/pickups/per-player rack), no version bump (v is stamped, never gated). Verified via playwriter against local wrangler dev: lobby → fire → mirror → stick-and-ride → blast double kill (+169 mirrored) → cache restock banner; dead soldiers cannot fire (learned by being devoured mid-test).
- Kill-charged focus + nape strike (grilled decisions, 2026-07-10): the focus meter no longer regens — it is a 3-kill charge (any weapon counts, including the strike's own kill), Q is dead until full, and a TAP opens one fixed 3-real-second slow-mo window (unchanged 0.3× scale) that always runs its full course — releasing Q cannot end it (revised same day: the original hold-to-sustain window made a tap deplete the whole charge in one tick); only the strike or an intermission cuts it short, and either way the charge is spent. Inside a window a nape within ~4°/60 m with clear line of sight becomes a lock — nape indicator glow (accepted texture-rule exception) plus an "F — NAPE STRIKE" crosshair prompt — and F (contextual; otherwise it still slashes) fires a homing pass-through dash in `src/sim/strike.ts`: 130 m/s sim-owned movement, invulnerable, hooks released, time snapped back to 1× (the molasses-to-blur cut IS the zoom), guaranteed kill on passage paying a speed-cap one-cut blade payout incl. the footballer 3× tier with zero blade wear (kill event weapon 'focus'), exiting ~10 m past at 26 m/s clipped short of buildings and ground. LMB/Space were rejected as strike inputs (they are hook/jump). HUD: 3-segment focus bar with per-kill charge flash, READY — Q pulse, KILL TITANS TO CHARGE empty hint; strike adds a radial speed-line overlay, FOV slam to 112, synth swoosh over a slowed blade sample, and a focus-ready ping. focusCharge joined SavedRun additively (pre-strike saves load with an empty meter); a strike that clears the wave is cleaned up at the intermission (pinned by test). Co-op untouched: stepCoopClient never runs the focus path and body.coop hides the focus row. Verified via playwriter: charge 0→3, lock, 772-point dash kill, intermission cleanup.
- [Shifter boss fights](tickets/009-shifter-boss-fights.md) (grilled decisions, 2026-07-13) — the Nine Titans as milestone bosses every 5th wave of Wave Survival (solo v1): one framework + nine BossSpecs on a fixed ladder (Beast wave 5 → Founding wave 45, repeating HP-scaled), damage only on the lit Weak Point by blade or blast (part HP pools, immovable killSpeed, breaks reuse Staggered and disable linked abilities, Plated parts cracked by spears — ADR 0002), lone gate arrival with relentless aggro, segmented brass boss bar + lit-part glow + full spectacle beats, breaks pay 250×chain with a 2000-base Flawless-capable kill payout. Models arrive from the parallel Blender session as public/models/<spec-id>.glb with capsule-rig fallback.
- The Nine boss-rush mode + cardinal resupply stations (user requests, 2026-07-13; addendum in ticket 009) — `bossrush` GAME_MODES entry fields the Shifter ladder one boss per wave (upgrades between, HP-scaled laps past the Founding); Arena.station became `stations`: plaza + one per cardinal on open street (~0.62R), placed by a deterministic rng-free citygen post-pass so seeds keep their layouts; resupply/prompt/minimap/visuals all speak `nearestStationDist`. Co-op parity needs `pnpm server:deploy`.
- [Commendations](tickets/010-commendations.md) (grilled decisions, 2026-07-13) — lifetime
  achievements riding the sim event bus: `src/sim/commendations.ts` tracker (feats + tiered
  lifetime counters over the 51-type GameEvent union), queued corner toast + chime in-run,
  menu panel with progress, `aot-odm-commendations` localStorage via the StorageLike seam.
  Solo modes only (co-op later); record only, nothing mechanical; playground/autopilot
  excluded. ~33-entry roster incl. per-Shifter "Felled" set with All Nine Silenced capstone.
- Matchday game mode (user request, 2026-07-09): second entry in GAME_MODES, solo, "every titan on the pitch is a footballer". modes.ts extracted the wave-clear/upgrade loop into a waveLoop(composition) skeleton both modes spread; matchdayComposition in waves.ts rolls a 50/50 striker/captain roster at signature heights with the usual escalation curve. announceWave brands every wave "Matchday · Wave N" in this mode. Menu card picked up from the registry automatically; mode persists via localStorage and ?mode=matchday.
- Footballers REMOVED (user decision, 2026-07-13, Norway–England having been played): the Matchday mode, the striker/captain TitanKinds, the 3x jackpot tier, the every-3rd-wave event, strikers.ts, and the two non-CC0 face photos are all gone — the art credits are 100% CC0/public-domain again. SAVE_VERSION bumped 2→3 (old runs discarded); unknown mode ids fall back to Wave Survival; historical leaderboard scores stand. The waveLoop(composition) skeleton the mode drove remains the mode-registry pattern. Git history at this entry's date holds the full event-titan pattern if a seasonal guest is ever wanted.

## Not yet specified

- Additional game modes on the new registry (time trial with checkpoint rings, parkour with flags/markers) and per-mode best stats.
- Additional titan variants (crawlers) and rare events (abnormal stampede); the boss/armored finale graduated into ticket 009.
- Post-processing polish (motion blur/speed lines, bloom on nape glow) if perf budget allows.
- Night gameplay flavor (residual from the day/night idea): titans slow at night per lore (they need sunlight); could become a night-survival wrinkle or its own GameMode entry.
- Mobile/touch support — unclear it can ever feel good for this control scheme.
- A canal district and gate structures in the wall for more Trost fidelity.

## Out of scope

- Multiplayer — redrawn into its own effort on 2026-07-09: see
  [the multiplayer map](map-multiplayer.md). This map stays the authority for the solo game.
- Licensed AoT assets/IP names in shipped UI — original low-poly look instead.
